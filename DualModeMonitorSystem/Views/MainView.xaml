<Window
    x:Class="DualModeMonitorSystem.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:DualModeMonitorSystem.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:vm="clr-namespace:DualModeMonitorSystem.ViewModels"
    Title="MainWindow"
    Width="1200"
    Height="750"
    d:DataContext="{d:DesignInstance vm:MainViewModel,
                                     IsDesignTimeCreatable=True}"
    mc:Ignorable="d">
    <Grid Background="#1E222B">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border
            Background="#272E3B"
            BorderBrush="#374151"
            BorderThickness="1">
            <DockPanel>
                <TextBlock
                    Margin="25,10"
                    FontSize="30 "
                    Style="{StaticResource NavigationFont}"
                    Text="工业温湿度监控系统" />
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <TextBlock Style="{StaticResource NavigationFont}" Text="运行模式:" />
                    <ToggleButton
                        Margin="5,10,10,10"
                        IsChecked="True"
                        Style="{StaticResource MaterialDesignSwitchSecondaryToggleButton}" />


                    <TextBlock Style="{StaticResource NavigationFont}" Text="Mes集成模式" />
                    <Border
                        Margin="10,0,2,0"
                        Style="{StaticResource StatusIndicatorStyle}"
                        Tag="Normal" />
                    <TextBlock Style="{StaticResource NavigationFont}" Text="系统正常运行中" />


                    <materialDesign:Chip
                        Margin="10,0"
                        Command="{Binding}"
                        Content="James Willock"
                        Foreground="White"
                        Style="{StaticResource MaterialDesignOutlineChip}">
                        <materialDesign:Chip.Icon>
                            <Image Source="{Binding ImageUrl}" />
                        </materialDesign:Chip.Icon>
                    </materialDesign:Chip>
                </StackPanel>
            </DockPanel>
        </Border>
        <Grid Grid.Row="1" Background="#272E3B">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border BorderBrush="#374151" BorderThickness="1">
                <ListBox
                    Width="200"
                    Margin="10,0"
                    Padding="5"
                    BorderBrush="#374151"
                    FontFamily="Inter, system-ui, sans-serif"
                    FontSize="25"
                    Foreground="White"
                    ItemsSource="{Binding MenuList}">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel HorizontalAlignment="Center" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style BasedOn="{StaticResource MaterialDesignListBoxItem}" TargetType="ListBoxItem">
                            <Setter Property="materialDesign:ListBoxItemAssist.SelectedFocusedBackground" Value="#9CCDE2" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border
                                            x:Name="ItemBorder"
                                            Width="180"
                                            Padding="10"
                                            BorderThickness="0.5"
                                            CornerRadius="6">
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="MouseLeftButtonUp">
                                                    <i:InvokeCommandAction Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" />
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                            <TextBlock Text="{Binding Title}" />
                                        </Border>
                                        <!--  触发器：处理选中、悬停状态  -->
                                        <ControlTemplate.Triggers>
                                            <!--  选中状态  -->
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="ItemBorder" Property="Background" Value="#4685B9" />
                                                <Setter Property="Foreground" Value="YellowGreen" />
                                                <!--  选中时蓝色背景  -->
                                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#3E4A61" />
                                            </Trigger>
                                            <!--  鼠标悬停  -->
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ItemBorder" Property="Background" Value="#3E4A61" />
                                                <!--  悬停时稍亮背景  -->
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Border>
            <ContentControl Grid.Column="1" prism:RegionManager.RegionName="MainContentRegion" />
        </Grid>
    </Grid>
</Window>
