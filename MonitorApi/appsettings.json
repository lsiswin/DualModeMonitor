{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "ConnectionStrings": {
        // Data Source 指定数据库文件路径（输出目录生成 DualModeMonitorDB.db）
        "SqliteConnection": "Data Source=DualModeMonitorDB.db;Foreign Keys=True;Cache=Shared"
    }
}
